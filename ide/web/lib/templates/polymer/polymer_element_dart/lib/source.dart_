library ``sourceName``;

import 'package:polymer/polymer.dart';

@CustomTag('``tagName``')
class ``className`` extends PolymerElement {
  @published String who = "";
  @observable String greeting = "";

  ``className``.created() : super.created();

  @override
  void ready() {
    super.ready();
  }

  @override
  void attached() {
    super.attached();
    greeting = "Hello, $who!"; 
  }
}
