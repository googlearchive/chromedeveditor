
import 'package:polymer/polymer.dart';
import 'package:paper_elements/paper_toast.dart';

@CustomTag('sample-app')
class SampleApp extends PolymerElement {
  SampleApp.created() : super.created();
  
  void showToast() {
    PaperToast toast = $['toast'];
    toast.show();
  }
}
