library x-``PolymerElement``;

import 'package:polymer/polymer.dart';

@CustomTag('x-``projectName``')
class ``projectName`` extends PolymerElement {
  @published String who = "";
  @observable String greeting = "";

  ``projectName``.created() : super.created();
  
  @override
  void ready() {
    super.ready();
  }
  
  @override
  void enteredView() {
    super.enteredView();
    greeting = "Hello, $who!"; 
  }
}
